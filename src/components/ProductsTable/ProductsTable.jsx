import { useReactTable } from '@tanstack/react-table';
import { TableContainer } from './ProductsTable.styled';

// eslint-disable-next-line react/prop-types
const ProductsTable = () => {
  return <TableContainer></TableContainer>;
};

export default ProductsTable;
